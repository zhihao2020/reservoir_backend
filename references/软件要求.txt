1.网格划分，输入边界信息（边界坐标），井位置坐标，网格尺寸(dx,dy,dz) 得到所有网格的序号和位置坐标
后面这些网格是饱和度场、压力场的网格载体。任何一个网格有序号i，坐标x,y,z 压力p 饱和度so sg sw，渗透率k，孔隙度 φ
2. 输入每一个时刻t下井位置坐标下的压力数值，边界压力和流量数值，
得到所有网格的压力数值。
3.输入每一个时刻t下井位置坐标下的饱和度数值，边界压力和流量数值，
得到所有网格的饱和度(油气水)数值。
4.每一个时刻t下
基于每一个网格的压力，饱和度，流量参数，通过渗流微分方程，获得每个网格的渗透率和孔隙度数值

软件部分就是这四个场数据：压力场、饱和度场（油气水）、物性场(渗透率，饱和度)

因为压力数据，饱和度数据是传感器测试的