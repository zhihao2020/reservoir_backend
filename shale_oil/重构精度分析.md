# 页岩油 CO₂ 吞吐场重构精度分析

> 针对 `shale_oil`（GEM 组分模型导出，15×15×15，30 cm 立方）的场重构结果与改进记录。

## 一、数据特征

| 项 | 值 |
|---|---|
| 模型 | GEM Peng–Robinson，14 组分，120 ℃，20 MPa |
| 几何 | 15×15×15，格子 0.02 m，k=0 底 / k=14 顶 |
| 孔隙度 | 0.05（常值） |
| 渗透率 | 0.03 mD（常值，近均质） |
| 注入 | INJ 井注纯 CO₂，8.333e-8 m³/s（储层条件） |
| 生产 | PROD1–4 采油，井底压力下限 19 MPa |
| 测点 | 16 个，位于 k=4 与 k=10 两层、i/j ∈ {4,7,10} 的环带（中心 (7,7) 无测点，是注入井所在） |

关键物理：**CO₂ 在 20 MPa 下超临界、高度溶于油**（组分行为）。注入的 CO₂ 大部分溶解进油相（使油溶胀），仅少量以自由气相存在，且自由气相**重力分异到模型底部**（k=0–2）后随时间逐渐溶解消失（630 天后全场 sg=0）。

## 二、重构结果（改进后）

| 场 | 相对误差 | 说明 |
|---|---|---|
| k（渗透率） | **3.1%** | P1 平滑权重 0.15→2.0 + P2 relperm 联合反演，从 34.3% 降下来 |
| p（压力） | **0.1%** | 平滑全局场，插值足够 |
| phi（孔隙度） | **0.0%** | 常值场，精确 |
| sw | 0 | 无注水，恒 0 |
| so / sg | **4.3%** | 见下：测点盲区 |

累计：k 相对误差 **34.3% → 3.1%（−91%）**。

## 三、so/sg 4.3% 误差的根因（测点盲区，非算法）

逐时间步 RMSE 显示：so/sg 误差**全部集中在头 ~10 步（30–330 天）**，之后精确为 0。

诊断结论（见 `comparison/plume_vs_probes.png`）：

1. **16 个测点在全部 120 步上 sg 观测恒为 0、so 恒为 1**——测点从未"看到"羽流。
2. 真值羽流集中在**底层**：k=0–2 层 `mean(sg)=0.456`、49% 格子 sg>0.1；而测点所在的 k=4/10 层 `mean(sg)=0.072`、仅 7.8% 格子 sg>0.1。
3. 压力/渗透率/孔隙度是**平滑全局场**，16 个测点足够；饱和度是**局部突变场**（底部自由气羽流），16 个错位测点结构性地漏掉了它。

因此：**任何插值/克里金算法都无法从"全 0"观测重构出羽流**——信息本身不存在。这是测点布设问题（数据盲区），不是重构算法问题。进一步叠加黑油前向模型或注入量约束（P3/P4）反而会更差：黑油模型把 CO₂ 当永久气相，而真值中自由气溶解消失（630 天后 sg=0）；累计注入量 ≈ 25.9 m³ 与自由气体积 ≈ 1e-4 m³ 相差 5 个数量级（CO₂ 溶解进油），"注入=自由气"的守恒不成立。

## 四、改进建议

1. **测点布设（最高杠杆）**：把测点放到**底层 k=0–2**（羽流所在），而非 k=4/10 的中部环带；建议底部 3 层每层 3×3 或 4×4 网格。这将直接消除 so/sg 的 4.3% 误差。
2. **组分一致性**：若饱和度精度对项目关键，场重构需升级为**组分模型**（追踪 CO₂ 组分质量：自由气 + 溶解气），黑油三相模型无法刻画 CO₂ 溶解/溶胀/密度反转的相行为。这是较大的架构升级，需单独立项。
3. **k/p/phi 已达标**：3.1% / 0.1% / 0.0%，无需继续投入。

## 五、已落地的代码改进

| 阶段 | 改动 | 效果 |
|---|---|---|
| P1 | `rock.py` 平滑权重 0.15→2.0（`2.0*(log_k - log_k[nearest])`） | k 34.3%→16.7% |
| P2 | `case.yaml` 增 `relperm` 联合反演（`[nw,no,ng,krw_end,kro_end,krg_end]`） | k 16.7%→3.1% |

relperm 反演结果 `{nw:2, ng:2, krw_end:0.3, kro_end:1.40, krg_end:0.9}`：`kro_end` 从默认 0.8 上调到 1.40，是黑油模型对"CO₂ 溶胀油"这一组分效应的等价补偿，使流动度模型与组分数据吻合，从而把 k 反演再收紧 5 倍。

P3（前向模型一致性）、P4（累计注入量羽流约束）**不适用**本组分案例，未实施。

## 六、CO₂ 组分追踪升级 + 测点迁移（后续迭代）

### 6.1 CO₂ 组分追踪（黑油三相 → 自由气 + 溶解气）

- `BlackOilParams` 增 `rs_slope`（亨利定律 `Rs = rs_slope·p`）、`bo_slope`（溶胀，预留）；新增 `solution_gas_ratio(p, params)`。
- `pipeline.py` 输出 `rs`（溶解气比 `=rs_slope·p`）与 `co2`（总 CO₂ 组分 `=sg+Rs·so`）。
- `case.yaml` 配 `rs_slope: 9.84e-6` → **Rs ≈ 187.5 m³/m³，与数据估算 187 精确吻合**（此前黑油模型完全漏掉这 99.9% 的溶解气）。
- **反演保持分相平衡**：曾把气相残差改成 CO₂ 组分残差，但本案例油相主导、Rs 巨大，组分残差 ≈ `Rs×油相残差`（冗余），且把 q_scale 放大 30 倍，k 退化到 12.77%。故 k 反演（流量）与 CO₂ 追踪（组分）分离。

### 6.2 测点迁移到底层 k=0/1/2（27 个，与井位对齐）

测点从 k=4/10 迁到底层，10/27 测点现在观测到 sg>0（最高 0.98），盲区已修复。但：

| 场 | 迁移前（16 测点） | 迁移后（27 测点） | 说明 |
|---|---|---|---|
| p | 0.1% | 0.2% | 仍准 |
| so / sg | 4.3%（漏报） | **4.3%（过报）** | 克里金把底层羽流抹到全模型；测点对羽流过采样（均值 0.34 vs 真值 0.15） |
| k | 3.1% | **均值 0.00%、空间变异 16–19%** | 羽流进入饱和度输入后，k 反演用空间变化 k 拟合局部气流（真值 k 常数） |

**根因**：羽流是**局部、瞬态、细尺度**特征（重力分异 + 气油过渡带），普通克里金 + 平稳反演无法刻画。测点迁移是必要但非充分——真正的修复是**前向模型（P3）**：用带溶解气的扩展黑油模型前向模拟羽流（质量守恒），而非克里金插值。Rs 扩展已是前向模型的基础。

## 七、前向模型（P3）落地 + 羽流输运路径查清（2026-09-23）

### 7.1 已落地（均通过回归测试）

| commit | 改动 | 效果 |
|---|---|---|
| `cf7ea33` | 前向模型升级 Peaceman BHP 驱动井 + 解压力（`solve_pressure_peaceman` / `well_cell_rates_peaceman`） | 自由气 1.7 → 120 格（~70×），压力解出注入峰值 |
| `253ea14` | `krg_floor` 诊断参数（气相相对渗透率下限，默认 0 向后兼容） | 诊断气体流度 |
| `7d7ae9f` | `_implicit_compositional_step` sg↔Rs 有界主变量切换 | 主变量始终有界，Newton Jacobian 不再因无界 C 奇异；守恒测试通过 |

### 7.2 底部羽流输运路径（用 observations.csv 真值数据查清）

day30 底部 27 测点：**10 个 sg>0.05，且 sg ≈ 0.80–0.98（高饱和、近纯气）**，其余 17 个 sg=0。day30 底部压力 19.0023–19.0658 MPa，注入井 k=8 峰值 19.103、k=0 生产 19.034（69 kPa 井压差）。

**结论：底部羽流是「注入的过量自由气以气相 Darcy 下流到底部累积」，不是「溶解 CO₂ 随油下流再析出」**——后者贡献很小（Rs_sat 139→113 压降 0.2 MPa 只析出 ~5%）。此前文档「溶解-析出是主机理」的判断需更正。

| 环节 | 真值 | 模型 |
|---|---|---|
| 注入井自由气 sg | ~0.71 | ~0.071 |
| 气体流度 λg | 高（krg≈0.9） | 低（krg≈0.006→λg≈322） |
| 自由气下流到底 | ~30 天 | ~72 天（慢 2.4×） |
| 底部累积 | sg 0.8–0.98 | 无（堆顶部） |

### 7.3 欠产 ~1000× 的三层根因

1. **C 裁剪丢质量**：线搜索 `C_new` 上界 `1/Bg·(1-sw)` 把超量 CO₂ 直接丢。放开后自由气 120→466 格、单格 `C.sum()==注入量` 精确守恒。
2. **气体流度太低**：SGT 表 `sg≤0.05→krg=0`（残余气），模型 sg~0.07→λg≈306 vs 真值 sg~0.71→λg≈45000（差 160×），排不掉多余 CO₂。
3. **压降/注速偏**：压降 0.020 vs 真值 0.222 MPa（65× 偏小）、注速偏快 6×。根因是「真值注入井 sg≈0.71 → 油相相对渗透率暴跌到 kro=0.0153、有效流度 7.65」vs 模型用初始 λo=500。**不是可压缩拟压力（修正仅 0.6%）也不是 geofac（0.34 只差 1.7×）**。

### 7.4 闭环根因与结论

三条根因构成**「饱和 ↔ 流度 ↔ 注速」闭环**：C 守恒 → 注入井累积自由气到 sg≈0.71 → 气体流度高 → 下流到底部羽流 → 油相流度暴跌 → 压降/注速自纠。OPM/MRST 的 fully-implicit（压力+饱和度+井耦合）正是这样自然算出这条链；本项目的解耦做法（压力解用当前流度 + 固定井速率）解不了这个闭环。

**后续方向**：若要自由气沉底，需 fully-implicit 耦合（压力+饱和度+井同时解），属架构级立项，非单点 bug 修复。当前 so/sg 最优仍是克里金 ~4.3%（集中在 day30 羽流过采样）；p/phi/k/sw 已达标（0–0.2%）。

### 7.5 GEM 井模型关键参数（shailoil.dat）

- `GEOMETRY K 0.003 0.34 1.0 0.0`（rad/geofac/wfrac/skin）；INJ `OPERATE MAX BHF 0.0072`（`FLOW-FROM 'SURFACE'`，即 8.33e-8 m³/s 是地面体积）。
- `PERMI *CON 0.03` / `PERMJ EQUALSI` / `PERMK EQUALSI * 0.2`（kz=0.2·kx）。
- 注入井 11 层（GEM k=1..11），生产井 PROD1/2/3/4 位于四角，`OPERATE MIN BHP 19000`。

## 八、fully-implicit 落地 + 代码审查修 bug 后的最终精度（2026-09-24）

### 8.1 前向模型（--model compositional）day30 自由气

| 模型 | 自由气 sg_sum | 底部 k0-2 均值 | 顶部 k13-14 |
|---|---|---|---|
| 真值 GEM | 488 格 | 0.456 | ~0 |
| 组分前向（修 bug 前） | 566 格 | 0.208 | 0.157 |
| 组分前向（修 bug 后 `53b1c7a`） | **549 格** | 0.202 | 0.151 |
| 克里金重构 | 1182 格 | 0.370 | — |

- **前向模型已比克里金更接近真值**（549 vs 1182，真值 488，1.13× 略过）。
- **但自由气仍未沉底**：真值全在底部（0.456），模型近似均匀（底部 0.20 / 顶部 0.15）。根因是「饱和↔流度↔注速」闭环未完全闭合——气体没在注入井累积到高饱和、沿路油欠饱和把自由气吞了、残余气 `krg=0` 让气体流不动。

### 8.2 代码审查（对抗 workflow）确认并修复 6 个真实 bug（`53b1c7a`）

1. `injects_gas` 每时间步标量只标井 0 → 每口井布尔 `(qg>0).any(axis=0)`。
2. 耦合步 `J_wp/J_cp` 注入井格用生产井公式 → 注入井 `∂r_sw/∂p=0`、`∂r_c/∂p=w·λt/Bg`。
3. 子步 Newton 不收敛时静默丢剩余区间 → 加 warnings。
4. BHP 模式 `so_hist/sg_hist` 用克里金 p → 改求解的 Peaceman p。
5. 缓存无版本静默过期 → 加 sha256 EOS 参数指纹。
6. `krg_floor` 只 tabular 生效 → Corey 也加。

### 8.3 性能（cProfile 定位真瓶颈）

- 真瓶颈是 **PR EOS 闪蒸**（Rs_sat 表构建 `np.roots`/`eigvals`，45s 一次性），不是矩阵/线性求解（0.14s/子步）。
- Cardano 三次求根 45s→13.6s（`f3eba29`）+ 落盘缓存 13.6s→50ms（`65ba7f8`）。
- 30 天全跑 ≈ 7 分钟（稳态 0.14s/子步 × 3000）。

### 8.4 气体流度处理：动力学溶解进 fully-implicit 耦合（2026-09-24）

**诊断（单步实跑确认）**：注入井为何不累积自由气，不是「油欠饱和吞气」，而是**瞬时平衡相劈分**把注入 CO₂ 全溶进油（Rs≈120 巨大），注入井 sg 被钉在 ~0.01。注入井的油其实已饱和（`Rs_act/Rs=1.0`），欠饱和吞气只发生在**沿路**（自由气在流动途中重新溶解）。

| 模型 | 注入井 sg | 机理 |
|---|---|---|
| 平衡（`k_diss=0`） | **0.010** | 注入 CO₂ 瞬时溶进油（Rs≈120），几乎无自由气 |
| 动力学（`k_diss>0`） | **0.078** | 注入 CO₂ 以自由气形式停留（溶解速率有限），**7.7× 更多自由气** |

**实现**：新增 `_implicit_compositional_pressure_kinetic_step`（`forward.py`）——把动力学溶解的 `(sw, C, Cd)` 三守恒方程与 Peaceman 压力方程**同解**，即 `(p, sw, C, Cd)` 四变量 fully-implicit 牛顿步。`Cd`（溶解 CO₂）以速率 `k_diss` 弛豫到平衡 `Rs·No`，注入 CO₂ 在注入井（停留时间 ≪ 1/k_diss）保持自由气、沿路才溶解。此前动力学路径走 Picard 定点迭代、不耦合压力；现在 `bhp_mode and kinetic` 也走 coupled 步。块前代换 δp → δsw → (δC, δCd)，(C, Cd) 因自由气劈分同阶耦合**必须联解**（继承 `_implicit_kinetic_step` 的结论）。

**代码审查（对抗 fork）确认并修复**：

1. `J_pw/J_pc/J_pd`（压力方程对 sw/C/Cd 的井对角反馈）符号反了（`bhp−p` 应为 `p−bhp`）且从未被块求解使用 → **删除死代码**（连带 `dlam_t_dsw/dC/dCd` 差分）。
2. `J_wC/J_wCd`（水方程对 C/Cd）同样组装了但未用 → 删除。
3. `J_dp` 缺溶解源 `k_diss·(Rs(p)·No−Cd)` 的 p 导数 → 该项 `k_diss·No·dRs/dp·φV ≈ 1e-17`（k_diss≲1e-5）比井项小 ~10 个量级，Rs(p) 按迭代滞后重算即可，**留空但注释说明**。
4. 守恒回归测试原先只断言 `accum>0` → 改为断言 `ΣφV·ΔC ≈ (Σqg + ΣRs_act·qo)·dt`（净注入 = 累积，散度零通量贡献为 0）。

**day30 对比（`--model compositional`，BHP 模式，`.venv` scipy 1.16.3）**：

| 模型 | 自由气 sg_sum | 底部 k0-2 | 顶部 k13-14 | 注入井 sg |
|---|---|---|---|---|
| 真值 GEM | 488 格 | 0.456 | ~0 | ~0.71 |
| 旧平衡（定 BHP + `k_diss=0`，`53b1c7a`） | 549 格 | 0.20 | 0.15 | ~0.01 |
| 定流量·固定源 + `k_diss=0` | **63 格** | 0.0002 | 0.0012 | 0.065 |
| 定流量·固定源 + `k_diss=1e-6` | **1500 格** | 0.010 | 0.030（顶部重） | 0.080 |
| **定流量·bhp_inj 变量（完整井雅可比）+ `k_diss=0`** | **0 格** | 0.0000 | 0.0000 | 0.0000 |
| **定流量·bhp_inj 变量（完整井雅可比）+ `k_diss=1e-6`** | **1857 格** | 0.046 | 0.022（底部重） | 0.055 |

**关键发现**：定流量修正（8.5）把注速从 5.90× 偏快拉到精确后，**自由气量反而从 549 掉到 63**——说明旧模型的「注速偏快 6×」一直在**掩盖**相劈分的过度溶解（Rs≈120 把 CO₂ 几乎全溶进油）。定流量是**必要但不充分**的修正：它修对了注速，却暴露了真正的病根是**相劈分过度溶解**。

**k_diss 标定（bhp_inj 变量 + 完整井雅可比，day30）**：

| k_diss | sg_sum | 底部 k0-2 | 顶部 k13-14 | 注入井 sg |
|---|---|---|---|---|
| 1e-6 | 1857 | 0.046 | 0.022 | 0.055 |
| 3e-6 | 1236 | 0.042 | 0.008 | 0.055 |
| 1e-5 | 926 | 0.039 | 0.003 | 0.055 |
| 真值 | 488 | 0.456 | ~0 | ~0.71 |

- **自由气量可标定**：k_diss↑（溶解更快）→ sg_sum↓（1857→926→488），~2e-5 可落在真值 488。
- **顶部可修**：k_diss↑ → 顶部 0.022→0.003（≈真值 ~0）。
- **注入井 sg 钉死 ~0.055、底部 ~0.04**：与 k_diss 无关。这是黑油溶液气模型的**本质局限**——注入井自由气被「自由气通量（λg 随 sg 升高）」排走，稳定在 sg≈0.055（自由气通量 = 注入量）；而真值 sg=0.71 是**气相驱替油相**（体积置换）的结果，不是溶解平衡。要复现「注入井高饱和→自由气下沉到底」，需把注入当**独立气相驱替**而非「组分溶解」，属架构级建模（接近 MRST co2lab 的非润湿气相驱替 + 溶解源项），非参数标定能解。

### 8.5 注入井控制模式修正：BHP 控制 → 定流量控制（2026-09-24，依据 reference_projects）

**GEM deck `examples/shailoil.dat` 的井控**（第 143–149 行）：

| 井 | 控制 | 说明 |
|---|---|---|
| INJ | `OPERATE MAX BHF 0.0072`（=8.33e-8 m³/s 地面）+ `MAX BHP 20000` | **定流量**注入（BHP 是结果，不是输入） |
| PROD1–4 | `OPERATE MIN BHP 19000` | **定 BHP**（19 MPa 下限） |

即：**注入井是定流量（rate-controlled），生产井是定 BHP（BHP-controlled）**。`cmg_wells.csv` 印证：INJ 每步 `q_g=8.333e-8 m³/s`（地面）、`pw=19.325 MPa`（是定流量注入的结果，不是约束）。

**OPM `StandardWell_impl.hpp`（注入分支）印证注入井速率公式**：`total_mob_dense = Σ mob`（**总流度** λt，非气相 λg）+ 注入组成 `cmix_s` 决定相分配。所以本项目 `qg = WI·λt·(bhp−p)/Bg` 的**总流度取法是对的**，错在**控制模式**——此前把注入井当定 BHP（19.325 MPa 固定），算出的注速偏快：

| 模型 | 注入井地面气速率 | 相对真值 |
|---|---|---|
| 真值 GEM | 8.33e-8 m³/s | — |
| 旧（定 BHP 19.325 MPa） | **4.91e-7 m³/s** | **5.90× 偏快**（≈doc 7.3 的「注速偏快 6×」） |
| 新（定流量 `well_qg_fixed`） | **8.33e-8 m³/s** | **1.00× 精确** |

**实现**：`_implicit_compositional_pressure_step` / `_implicit_compositional_pressure_kinetic_step` 加 `well_qg_fixed`（注入井固定地面气速率）。`rate_controlled=True` 时注入井按 GEM `OPERATE MAX BHF` **定流量**：注入井 BHP `bhp_inj` 作为**变量**，由速率约束方程 `Σ WI·λt·(bhp_inj−p) = qg_target·Bg` 解出（保留井筒 drawdown，生产井保持 BHP 驱动）；`None` 时向后兼容旧的 BHP 驱动。**完整井雅可比**：注入井 BHP 对 C 方程的耦合 `∂r_c/∂bhp_inj = −D_inj/Bg` 进 δC 求解（Schur 补消除 bhp_inj）。守恒回归测试 `test_compositional_pressure_step_rate_controlled` 锁定「固定注入 = 累积」。

> 注：定流量修正让注速精确（5.90× → 1.00×），但它**暴露**了相劈分过度溶解（自由气 549→63），真正的病根是溶解模型而非注速——见 8.4 的 day30 对比。scipy 1.17.1 的 `gmres` access violation 已定位为**黑油步 `_implicit_black_oil_step` 把解耦的 `bmat([[J_ww,None],[None,J_ss]])` 当 2n×2n 联解**（零块让 GMRES 失稳），已拆成两个 n×n 独立解；并建 `.venv` 按 `requirements.txt` 钉 scipy==1.16.3。

> 附（完整定流量模型的 bug 修复历程）：完整定流量模型（bhp_inj 变量 + 速率约束 + Schur 补）最初出现**质量不守恒**——CO₂ 累积量随子步加速到 ~3.8× 注入量，平衡/动力学反号（sg_sum 3258/71）。逐步排查：散度算子守恒（Σflux=0）、标量速率约束残差（比 n 格向量残差小 ~200×）被 `0.1·r0` 判据淹没；试了加严判据（不收敛）、增广矩阵直接解（不定系统慢）均无效。**真 bug 是缺完整井雅可比的 `B_c = ∂r_c/∂bhp_inj = −D_inj/Bg` 项**——C 方程对注入井 BHP 的耦合没进 Jacobian，Newton 收敛到非守恒解。补上后：**质量守恒恢复**（每步 dC ≈ 注入量）、`k_diss=1e-6` 的 day30 自由气从「顶部重」变为「**底部重**」（底部 0.046 > 顶部 0.022），即注入井 drawdown 开始把自由气往下压——正是「饱和↔流度↔注速」闭环里「注入井压降驱动自由气下流」这一环。





